{
  "name": "bombardier-flow-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Servidor local pra armazenar sugestões de edição dos UX flows do styleguide. LAN-wide, auth por token, persistência em JSON. Workflow open → in_review → applied/discarded.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "start": "tsx --env-file=.env src/index.ts",
    "dev": "tsx watch --env-file=.env src/index.ts"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "lowdb": "^7.0.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
